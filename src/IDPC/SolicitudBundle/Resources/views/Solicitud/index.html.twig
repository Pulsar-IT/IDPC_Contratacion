{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Solicitud list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fechacierre</th>
                <th>Estadosolicitud</th>
                <th>Estadoproceso</th>
                <th>Area</th>
                <th>Created_at</th>
                <th>Update_at</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('solicitud_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fechaCierre %}{{ entity.fechaCierre|date('Y-m-d') }}{% endif %}</td>
                <td>{{ entity.estadoSolicitud }}</td>
                <td>{{ entity.estadoProceso }}</td>
                <td>{{ entity.area.sigla }}</td>
                <td>{% if entity.createdat %}{{ entity.createdat|date('Y-m-d') }}{% endif %}</td>
                <td>{% if entity.updateat %}{{ entity.updateat|date('Y-m-d') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('solicitud_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('solicitud_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('solicitud_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
