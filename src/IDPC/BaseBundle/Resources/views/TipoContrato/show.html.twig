{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TipoContrato</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Tipocontrato</th>
                <td>{{ entity.tipoContrato }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipocontrato') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tipocontrato_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
